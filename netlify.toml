[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — all paths serve index.html so client-side routing works
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

