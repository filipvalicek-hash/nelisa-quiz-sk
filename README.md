
  # UI Screen Replication Task

  This is a code bundle for UI Screen Replication Task. The original project is available at https://www.figma.com/design/fCZ2j9p90NrYSNwJfbJpWm/UI-Screen-Replication-Task.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  